import 'package:flutter/material.dart';

class MarqueScreen extends StatelessWidget {
  const MarqueScreen({Key? key}) : super(key: key);

  static String routeName = "/marques";

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
